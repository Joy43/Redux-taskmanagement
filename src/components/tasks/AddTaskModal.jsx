import Modal from "../ui/modal";

const AddTaskModal = ({ isOpen, setIsOpen }) => {
  return (
    <Modal isOpen={isOpen} setIsOpen={setIsOpen} title="ss joy redux"></Modal>
  );
};

export default AddTaskModal;
